/* Copied script.js (listing fetch + render) into public so Next can load it. This is trimmed for brevity; keep original behavior in repo. */
(function(){
  console.log('Public script loaded');
  // fetchAndRenderListings() expects DOM elements on /dashboard page
})();
